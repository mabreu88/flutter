# Repaso de Flutter / Dart

Repositorio con ejercicios y ejemplos para repasar conceptos básicos y avanzados de **Dart** utilizados en desarrollo con **Flutter**.

Este repositorio contiene una serie de archivos `.dart` con temas de sintaxis, estructuras de datos y programación orientada a objetos en Dart, que sirven como práctica y referencia para desarrolladores que están aprendiendo o reforzando conocimientos en Flutter.

---

## 📦 Contenido

Cada archivo Dart representa un ejercicio o tema de repaso, listados a continuación:

| Archivo | Descripción |
|---------|-------------|
| `01_hello_world.dart` | Ejemplo clásico de “Hola Mundo”. |
| `02_variables.dart` | Declaración y uso básico de variables. |
| `03_dynamic.dart` | Uso de tipos dinámicos en Dart. |
| `04_maps.dart` | Ejercicios con mapas (`Map`). |
| `05_list_iterables_y_sets.dart` | Listas, `Set` y otras colecciones. |
| `06_functions_and_parameters.dart` | Definición y uso de funciones con parámetros. |
| `07_classes.dart` | Clases básicas en Dart y creación de objetos. |
| `08_named_constructors.dart` | Constructores con nombre. |
| `09_setters_getters_assertion.dart` | Setters, getters y uso de `assert`. |
| `10_abstract_classes_enum_extends_implement.dart` | Clases abstractas, `enum`, herencia e implementación de interfaces. |
| `11_mixins.dart` | Uso de *mixins* para reutilizar funcionalidad. |
| `12_future_async_await.dart` | Programación asíncrona con `Future`, `async` y `await`. |
| `13_try_catch_on_finally.dart` | Manejo de excepciones con `try`, `catch`, `on` y `finally`. |
| `14_streams.dart` | Uso de `Stream` para manejo de secuencias de eventos asíncronos. |
| `15_stream_await.dart` | Ejercicios extendidos sobre flujos y `await` en `Stream`. |

---

## 🚀 Cómo usar

1. **Clonar el repositorio**

   ```bash
   git clone https://github.com/mabreu88/flutter.git
   cd flutter
